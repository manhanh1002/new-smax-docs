I will implement a seamless vertical border in the topbar that aligns perfectly with the sidebar, creating a unified grid-like layout.

### Implementation Steps

1.  **Update `components/layout/topbar.tsx`**:
    *   Restructure the header layout to separate the Logo area from the Main Content area.
    *   **Logo Container**: Set a fixed width of `w-64` (256px) on desktop (`lg` breakpoint) to match the sidebar's width.
    *   **Border**: Add a right border (`border-r`) to the Logo Container that uses the standard `border-border` color.
    *   **Spacing**: Remove the default padding from the `<header>` and apply it specifically to the inner containers to ensure the border sits exactly at the edge of the 260px mark.
    *   **Mobile Responsiveness**: Ensure these styles only apply on desktop (`lg`), reverting to a flexible layout on mobile to match the hidden sidebar behavior.

2.  **Update `components/layout/docs-shell.tsx`**:
    *   Adjust the Sidebar's sticky positioning to account for the new Topbar height (`h-16` / 64px).
    *   Change `top-14` to `top-16`.
    *   Update the height calculation from `h-[calc(100vh-3.5rem)]` to `h-[calc(100vh-4rem)]` to ensure the sidebar fits perfectly without scroll overlaps.

### Expected Outcome
*   **Visual Alignment**: A continuous vertical line will run down from the top of the screen, separating the Logo/Sidebar from the Main Content, creating a clean, professional "dashboard" aesthetic.
*   **Consistency**: The border color and width will perfectly match the existing design system.
*   **Responsiveness**: The layout will adapt gracefully to mobile screens, hiding the rigid grid structure where it's not needed.