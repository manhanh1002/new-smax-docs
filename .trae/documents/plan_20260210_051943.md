I will implement the missing Right Sticky Table of Contents and Bottom Related Docs Navigation features to match GitBook's functionality.

### 1. Right Sticky Table of Contents (TOC)

* **New Component**: Create `components/docs/table-of-contents.tsx` to render the sticky sidebar navigation.

* **Logic**:

  * Implement a `extractTOC` utility to parse all Markdown headings.

  * Create a `slugify` helper to generate consistent IDs for anchors.

* **Integration**:

  * Update `components/docs/doc-content.tsx` to:

    * Generate IDs for H1/H2/H3/H4/H5/H6 elements in the `parseContent` function.

    * Adopt a 2-column grid layout (Main Content + TOC) on desktop screens. On mobile screens, collapse TOC into a sticky burger icon with click to open full page with close button.

    * Pass extracted headings to the new TOC component.

### 2. Bottom Related Docs Navigation (Prev/Next)

* **New Component**: Create `components/docs/pager.tsx` to display "Previous" and "Next" navigation cards.

* **Logic**:

  * Create a `getPrevNext` utility in `lib/docs/utils.ts` that flattens the nested navigation tree from `lib/docs/nav.ts`.

  * Traverse the list to find the current page and identify its immediate siblings.

* **Integration**:

  * Add the `DocsPager` component to the bottom of `components/docs/doc-content.tsx`.

### 3. Verification

* **Visual Check**: Verify the TOC appears on the right for large screens and stays sticky during scroll.

* **Functional Check**: Ensure clicking TOC links smooth-scrolls to the correct section.

* **Navigation Check**: Confirm "Previous" and "Next" buttons correctly link to the adjacent pages in the hierarchy.

