Tôi sẽ thực hiện chuyển đổi cấu trúc Routing sang `/tai-lieu/[lang]/...` và tạo dữ liệu giả lập (mock data) như sau:

1.  **Tái cấu trúc Dữ liệu (Mock Data)**:
    *   Đổi tên `lib/docs/pages.tsx` thành `lib/docs/pages-en.ts`.
    *   Tạo mới `lib/docs/pages-vi.ts` với nội dung được dịch sang Tiếng Việt.
    *   Tạo `lib/docs/service.ts`: Export hàm `getDocPage(slug, lang)` để lấy dữ liệu đúng theo ngôn ngữ.

2.  **Tái cấu trúc Routing (Next.js App Router)**:
    *   Di chuyển `app/tai-lieu/page.tsx` -> `app/tai-lieu/[lang]/page.tsx`.
    *   Di chuyển `app/tai-lieu/[...slug]/page.tsx` -> `app/tai-lieu/[lang]/[...slug]/page.tsx`.
    *   Tạo `app/tai-lieu/page.tsx` gốc: Tự động chuyển hướng (Redirect) về `/tai-lieu/vi`.

3.  **Cập nhật Logic Hiển thị & Điều hướng**:
    *   Cập nhật `generateStaticParams` để hỗ trợ cả tham số `lang` ('vi', 'en') và `slug`.
    *   Cập nhật `Sidebar`, `MobileNav`, và `LanguageSelector` để các liên kết tự động thêm prefix ngôn ngữ hiện tại (ví dụ: đang ở `/vi` thì bấm vào "Quickstart" sẽ đến `/tai-lieu/vi/quickstart`).

Kế hoạch này đảm bảo URL rõ ràng (`/tai-lieu/vi/...`) và dữ liệu được phân tách, sẵn sàng cho việc tích hợp Supabase sau này.